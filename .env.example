# Optional. If set, the app uses this as the default OpenAI key.
# WARNING: Vite inlines VITE_* variables into the built JS bundle,
# so anyone who loads the page can read it. Only use for local dev.
VITE_OPENAI_API_KEY=
